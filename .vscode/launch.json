{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "Cargo build",
            // "program": "${workspaceRoot}/targer/debug/${workspaceFolderBasename}"
            "program": "${workspaceRoot}/target/debug/pixel-rs"
        }
    ]
}